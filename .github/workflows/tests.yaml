name: Run Flutter and Android Tests

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  flutter-tests:
    name: Run Flutter Tests
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.2' # Explicit Flutter version

      # Step 3: Install dependencies
      - name: Install Dependencies
        run: flutter pub get

      # Step 4: Run Flutter tests
      - name: Run Flutter Tests
        run: flutter test


  android-native-tests:
    name: Run Android Native Tests
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up JDK (Updated Java version to 17, ensuring compatibility)
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      # Step 3: Install Flutter SDK
      - name: Install Flutter SDK
        run: |
          git clone https://github.com/flutter/flutter.git /opt/flutter
          echo "flutterSdkPath=/opt/flutter" >> $GITHUB_ENV

      # Step 4: Clean Flutter build cache
      - name: Flutter Clean
        run: /opt/flutter/bin/flutter clean

      # Step 5: Resolve Flutter dependencies
      - name: Flutter Pub Get
        run: /opt/flutter/bin/flutter pub get

      # Step 6: Cache Gradle dependencies (Ensures build optimization)
      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # Step 7: Replace Flutter SDK path in settings.gradle (Ensures correct environment setup)
      - name: Update flutterSdkPath in settings.gradle
        run: |
          sed -i 's|\$flutterSdkPath|/opt/flutter|g' android/settings.gradle

      # Step 8: Grant execute permissions for Gradle Wrapper
      - name: Grant execute permissions for Gradle Wrapper
        run: chmod +x ./android/gradlew

      # Step 9: Verify Gradle and Flutter setup (Optional but recommended debugging step)
      - name: Verify Setup
        run: |
          /opt/flutter/bin/flutter doctor
          ./android/gradlew --version

      # Step 10: Run Android native tests
      - name: Run Android Native Tests
        working-directory: android
        run: ./gradlew testDebugUnitTest

      # Step 11: Run Android Lint (Optional quality check)
      - name: Run Android Lint
        working-directory: android
        run: ./gradlew lintDebug
